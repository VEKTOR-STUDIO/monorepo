# Copy to .env.local and fill in. Never commit real secrets.

# Optional: landing-only mode (bypass auth routes)
NEXT_PUBLIC_LANDING_ONLY=

# Optional, SOLO desarrollo: ver /dashboard y /admin sin login (se ignora en producción)
NEXT_PUBLIC_DEV_NO_LOGIN=

# Resend (emails)
RESEND_API_KEY=

# Supabase — proyecto PROPIO de Camargo Coach (no reutilizar el de otro cliente)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Stripe
STRIPE_PUBLIC_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Site URL for sitemaps (production)
SITE_URL=

# OpenAI (optional, if using libs/gpt.js)
OPENAI_API_KEY=
