// Shared utilities — populated as duplicated helpers across client apps are
// pulled in. Candidates:
//   - libs/api.js (axios instance + interceptors)
//   - libs/seo.tsx (Next metadata helpers)
//   - config helpers (currency formatters, plan resolvers, etc.)

export {};
